// const gui = new GUI();
      // const spacerFolder = gui.addFolder('Cube');
      // spacerFolder.open();
      // const spacerFolder2 = gui.addFolder('Cube2');
      // spacerFolder2.open();
      // const cameraFolder = gui.addFolder('Camera');
      // cameraFolder.add(camera.position, 'z', 0, 10);
      // cameraFolder.add(camera.position, 'x', 0, 10);
      // cameraFolder.add(camera.position, 'y', 0, 10);
      // cameraFolder.add(camera.rotation, 'y', 0, 3);
      // cameraFolder.close();
      // const lightFolder = gui.addFolder('roomLight');
      // lightFolder.add(roomLight.position, 'x', -10, 10);
      // lightFolder.add(roomLight.position, 'y', -10, 10);
      // lightFolder.add(roomLight.position, 'z', -10, 10);
      // lightFolder.add(roomLight, 'intensity', 0, 1);
      // lightFolder.addColor(roomLight, 'color');
      // // lightFolder.add(model, 'color', 0, 10);
      // lightFolder.open()